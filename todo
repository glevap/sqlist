1. вынести в пакет структуру, аналогичную dto request.RequestQuery
2. придумать адаптер под эту структуру, чтобы использование в repository не стало болью
3. вынести в аргументы билдера поле-префикс (search_) для фильтов??? префикс может поменяться, не сможем маппить поля
4. работа с CTE
5. пагинация

Следом будет допил под использование CTE и других историй, которые мы используем в запросах списочного вывода.
И переход на курсорную пагинацию.

6. Добавляем работу с Expr!
7. Нужно обрабатывать ситуацию, когда в WithFieldConfig переопределяется поле!!!
8. Хорошо бы добавить обработку и сохранение ошибки для ее последующего возврата из build-методов