1. [-] вынести в пакет структуру, аналогичную dto request.RequestQuery
   [это ответственность проекта и его репозитория]
2. [-] придумать адаптер под эту структуру, чтобы использование в repository не стало болью
3. [-] вынести в аргументы билдера поле-префикс (search\_) для фильтов??? префикс может поменяться, не сможем маппить поля
   [это ответственность конкретного репозитория]
4. [x] работа с CTE
5. [ ] пагинация
6. [x] Добавляем работу с Expr!
7. [x] Нужно обрабатывать ситуацию, когда в WithFieldConfig переопределяется поле!!!
8. [x] Хорошо бы добавить обработку и сохранение ошибки для ее последующего возврата из build-методов
9. [x] Проработать случай, когда в bool условии используется не column = true, а column is false!
    - добавлять отдельный функционал под данную историю не за чем. Squirrel конкатенирует
      sql c выражениями вида field = true / field = false, и такой SQL отлично отрабатывает.
      Найдутся еще кейсы, рассмотрю доработку.
10. [ ] В работе нам не требовался оператор OR в условиях запроса, но, он может однажды понадобиться:
        [x] метод Where
        [-] в applyPriority мы добаввляем where через and
        [ ] РЕАЛЬНЫЙ КЕЙС: поле bool null
        при фильтрации = true вернутся все записи с true,
        при фильтрации по = false (все остальные), вернется только записи с false,
        а записи с NULL остаются за бортом!
        [???] можно решать подобные кейсы за счет простейшей логики в цикле для ApplyFilter
11. [x] Переработать приоритизацию, т.к. нам нужно соблюдение наличия полей для работы индекса,
        а порядок в where не влияет на ситуацию.
12. [x] определиться с order/sort by
13. [x] доработать схему ApplyFilter
    - в repo/persons/new_recipient.go классика жанра:
        ```
        if after == "snils" {
        		builder.ApplyFilter(after, "persons.snils2bcd64(?)", val[0])
        	} else {
        		builder.ApplyFilter(after, val[0])
        	}
        ```
        добавь сюда еще if на определение cte/select == куча грязи
    - определение cte/select происходит в логике пакета - вопрос закрываем.
14. [ ] BuildCount() пока работает над полным sql (c cte вместе), но это работает на локалке
15. [ ] подумать над WhereIF
    - можно в where добавить проверку по значению, и не добавлять в фильтр данные,
      но это неочевидное поведение, адекватней отдельный идеоматичный метод
    - при добавлении метода обрати внимание, что, например, op == Expr подразумевает наличие
      выражения, а не значения (это args!). Надо будет прописать логику в зависимости от операнда
16. [x] настройки cte держать в экземпляре билдера для cte (aliasCTE, nameCTE...)
17. [x] приоритизация откровенно лишняя, убрать, освободить структуру билдера
18. [x] WithField & WithFields создают неоднозначность: когда указываешь поля в виде мультистроки (создается слайс len=1)!!!
19. [x] проверить работу CTE:
    - [x] когда их более 1 экземпляра
        - работать не будет, перезапишет cte текущий
            - нужен слайс структур cte []SQLBuilder
20. [ ] Подумать над методом OR для объединения условий в Where()

    ```
    // Or группирует условия в OR
    func (b *SQLBuilder) Or(conditions ...squirrel.Sqlizer) *SQLBuilder {
    	if len(conditions) > 0 {
    		b.whereConditions = append(b.whereConditions, squirrel.Or(conditions))
    	}
    	return b
    }

    // And группирует условия в AND (обычно не нужно)
    func (b *SQLBuilder) And(conditions ...squirrel.Sqlizer) *SQLBuilder {
    	if len(conditions) > 0 {
    		b.whereConditions = append(b.whereConditions, squirrel.And(conditions))
    	}
    	return b
    }
    ```

